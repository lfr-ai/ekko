{
  "$schema": "https://schemas.anthropic.com/mcp/servers.json",
  "mcpServers": {
    "gitnexus": {
      "type": "stdio",
      "command": "gitnexus",
      "args": ["mcp"],
      "cwd": "${workspaceFolder}",
      "description": "GitNexus MCP server for advanced git operations and repository analysis"
    },
    "context7": {
      "type": "http",
      "url": "https://mcp.context7.com/mcp",
      "description": "Context7 MCP server for library documentation lookup"
    },
    "shadcn": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "shadcn@latest", "mcp"],
      "cwd": "${workspaceFolder}/frontend",
      "description": "shadcn/ui MCP server for component registry"
    },
    "playwright": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest", "--isolated", "--headless"],
      "description": "Playwright MCP server for browser automation via accessibility snapshots"
    },
    "storybook": {
      "type": "http",
      "url": "http://localhost:6006/mcp",
      "description": "Storybook MCP server for component docs and interaction exploration"
    }
  }
}
