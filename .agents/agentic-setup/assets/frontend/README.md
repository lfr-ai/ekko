# Frontend capability blueprint

For a frontend or full-stack repository, copy the `frontend` profile's skills,
agents, and instruction/rule pairs. Preserve the layered source boundary
`domain → infrastructure → application → presentation → router`, with `lib` as a
shared leaf. Enable optional MCP servers only when their backing service is used.
