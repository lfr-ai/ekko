# Ekko agent knowledge base

Read `PROJECT.md` first for volatile project facts, then use these durable maps:

1. `architecture.md` — layers, boundaries, and composition roots.
2. `domain-glossary.md` — Ekko's ubiquitous language.
3. `flows.md` — audio, GraphQL, PII, prompt, and persistence flows.
4. `design-decisions.md` — deliberate choices that differ from generic templates.

Authoritative behavior lives in `openspec/specs/`; active rules live in
`.github/instructions/` and `.claude/rules/`; reusable procedures live in the
three mirrored skill trees. Verify symbols and impact against source and
GitNexus before editing.
