services:
  app:
    build:
      context: ..
      dockerfile: docker/Containerfile
      target: prod
    environment:
      EKKO_ENVIRONMENT: staging
    restart: unless-stopped
    networks:
      - backend

networks:
  backend:
