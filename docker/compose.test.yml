services:
  app:
    env_file:
      - path: ../.env
        required: false
      - path: ../.env.test
        required: false
    environment:
      VOICE_ENVIRONMENT: test
    command: ["sh", "-c", "uv run pytest -q || pytest -q"]
