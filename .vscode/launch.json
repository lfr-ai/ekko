{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run FastAPI (uvicorn)",
      "type": "python",
      "request": "launch",
      "module": "uvicorn",
      "args": ["voice.interaction.main:app", "--reload", "--host", "127.0.0.1", "--port", "8000"],
      "jinja": true
    }
  ]
}
{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "voice-bot: FastAPI (uvicorn)",
      "type": "python",
      "request": "launch",
      "module": "uvicorn",
      "args": [
        "voice.interaction.main:app",
        "--host",
        "127.0.0.1",
        "--port",
        "8000",
        "--reload"
      ],
      "env": {
        "PYTHONPATH": "${workspaceFolder}/src"
      },
      "cwd": "${workspaceFolder}",
      "justMyCode": true
    }
  ]
}
